OLLAMA_MODEL=llama3.1:latest

# If Ollama is running natively in WSL/Linux, leave this as 127.0.0.1 (default).
# If Ollama is running on Windows and you're using WSL, set this to your WSL gateway IP:
#   Run: cat /etc/resolv.conf | grep nameserver
# OLLAMA_HOST=172.31.x.x
